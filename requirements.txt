# pip install -r requirements.txt


# Core dependencies
requests==2.28.1
numpy==1.24.0
pandas==1.5.3
pyarrow==11.0.0
pyspark==3.3.2

# Snowflake dependencies
snowflake-connector-python==2.7.5
snowflake-sqlalchemy==1.3.0

# Testing dependencies
pytest==7.2.2
pytest-mock==3.10.0
unittest2==1.1.0

# Mocking dependencies
mock==4.0.3

# Other dependencies required for Airflow (if used)
apache-airflow==2.6.0
apache-airflow-providers-snowflake==3.0.1
apache-airflow-providers-http==3.0.1

# Optional (but might be used for integration and Snowflake-related functionality)
sqlalchemy==2.0.9
